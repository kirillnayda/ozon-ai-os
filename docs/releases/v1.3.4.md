# Ozon AI OS v1.3.4

- Исправлен `limit` запроса `/v1/analytics/stocks` с 1000 на подтверждённый контрактом максимум 100.
- После успешного запуска новой версии сам бот отправляет одно уведомление владельцу и сохраняет подтверждённую версию в `data/update-notified-version`.
- Обычные перезапуски той же версии не создают повторных уведомлений.
