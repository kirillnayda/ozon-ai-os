# Ozon AI OS v1.4.2

- `ads_cluster=null` нормализуется в `0 продаж/день` вместо блокировки всей синхронизации.
- Contract validation сообщает имя поля с неверным типом, не сохраняя и не показывая его значение.
