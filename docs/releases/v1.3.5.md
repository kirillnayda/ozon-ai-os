# Ozon AI OS v1.3.5

- `/ozon_contracts` дополнительно получает обезличенную схему `POST /v3/product/list`.
- Диагностика позволяет проверить наличие `sku` или `product_id` в подтверждённом ответе списка товаров перед формированием запросов остатков.
