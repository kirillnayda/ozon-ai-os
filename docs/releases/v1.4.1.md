# Ozon AI OS v1.4.1

- `/stocks_sync` показывает безопасную внутреннюю причину `ContractNotVerified`.
- Внешний JSON, значения товаров, заголовки и секреты по-прежнему не сохраняются и не отправляются в Telegram.
