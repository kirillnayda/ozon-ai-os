# Ozon AI OS v1.3.6

- Contract probe последовательно получает SKU из подтверждённого `result.items[].sku` метода `/v3/product/list`.
- До 100 SKU передаются в диагностические read-only запросы аналитики и FBO-остатков.
- Startup-уведомление запускается фоновой задачей после `deleteWebhook`, не блокируя инициализацию Telegram polling.
