# Ozon AI OS v1.3.3

- Updater повторяет Telegram-уведомление до пяти раз после успешного запуска сервиса.
- `/run/ozon-ai-os/update-status` отдельно фиксирует `notification_delivered` или `notification_failed`.
- `/ozon_contracts` извлекает числовые ограничения validation-error, исключая числа внутри идентификаторов и не сохраняя исходный текст ответа.
